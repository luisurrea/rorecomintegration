<%= render :partial => '/admin/merchandise/sub_header'%>

<h3>Editar Producto</h3>

<%= link_to 'Agregar Imagen', edit_admin_merchandise_images_product_path(@product), :class => 'button light' %>
<%= link_to 'Variantes', admin_merchandise_product_variants_path(@product), :class => 'button light' %>
<%= link_to 'Volver al producto',     admin_merchandise_product_path(@product), :class => 'button' %>
<%# content_for :sidebar do %>
  <div id="article-sidebar" class="sidebar">
    <%= form_for @product, :url  => admin_merchandise_product_path(@product),
                            :html => {:class => ''}  do |form| %>

      <%= render :partial => 'form', :locals => {:form => form } %>

      <div class=" generic_submit_button">
        <%= submit_tag "Actualizar",
                        :class  => "button",
                        :id     => "create_product_button" %>
      </div>
    <% end %>
  </div>
<%# end %>