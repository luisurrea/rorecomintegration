<%= render :partial => '/admin/merchandise/sub_header'%>

<h3>Nueva variante (<%= @product.name %>)</h3>

  <%= form_for @variant, :url  => admin_merchandise_product_variants_path(@product,@variant),
                          :html => {:class => ''}  do |form| %>
    <%= render :partial => '/admin/merchandise/variants/form', :locals => {:form => form}%>

    <div class=" generic_submit_button">
      <%= submit_tag "Crear",
                      :class  => "button",
                      :id     => "create_variant_button" %>
    </div>
  <% end %>

<p>
  Puede imaginarse una variante como un subproducto. Por ejemplo un cuchillo que tiene 5 variedades. Cada variedad es una variante.
</p>
