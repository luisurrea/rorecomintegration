<h1>Editar Cuenta</h1>


<%= form_for( @account, :url => admin_config_account_path(@account)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Atras', admin_config_accounts_path, :class => 'button' %>

