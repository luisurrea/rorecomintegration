<h1>Nueva zona de envio</h1>


<%= form_for(@shipping_zone, :url => admin_config_shipping_zones_path()) do |f| %>
  <%= render :partial =>  'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Atras', admin_config_shipping_zones_path, :class => 'button' %>

